<%= simple_form_for([cocktail, review], remote: true,  html: { class: "review_form" }) do |f| %>
  <%= f.input :rating, collection: 1..5, label: "Let's Rate it" %>
  <%= f.button :submit, class: "d-none" %>
<% end %>
