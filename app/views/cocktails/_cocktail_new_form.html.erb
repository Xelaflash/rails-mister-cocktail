<div class="col-lg-8 mx-auto">
  <%= simple_form_for(@cocktail) do |f| %>
    <%= f.error_notification %>
    <%= f.input :name, placeholder: "Mojito" %>
    <%= f.input :recipe, placeholder: "Add the recipe steps", as: :text %>
    <%= f.input :photo, label_html: { class: 'upload-photo'}, label: '📷 Upload a photo', as: :file, input_html: { class: 'd-none', id: 'photo-input'} %>
    <div class="preview">
      <%= image_tag "", width: 200, height: 250, class: "d-none", id: "photo-preview" %>
    </div>
    <%= f.button :submit, class: "btn btn-devise" %>
  <% end %>
</div>
